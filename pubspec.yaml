name: just_the_tooltip
description: The components for tooltips to be used in custom implementions.
version: 0.0.3
homepage: https://github.com/Nolence/just_the_tooltip

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  golden_toolkit: ^0.9.0

flutter:
  uses-material-design: true
